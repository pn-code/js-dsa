const TreeNode = require("./TreeNode");

function BST() {
    return {
        terminus: null,
        find(target) {},
        insert(value) {},
        delete(value) {},
    };
}

module.exports = BST;
